"""Opt-in indicator perf-budget regression gates.

Default ``pytest`` runs skip everything in this directory via the
``-m 'not perf'`` filter in ``pyproject.toml``. Run with:

    pytest tests/perf -m perf -v
"""
